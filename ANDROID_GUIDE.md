# 📱 دليل تشغيل البوت على أندرويد - الدليل الشامل

## 🎯 الطرق المتاحة

### ✅ الطريقة 1: خدمة سحابية مجانية (موصى بها)
**المميزات:**
- ✅ يعمل 24/7 بدون توقف
- ✅ لا يستهلك بطارية هاتفك
- ✅ لا يحتاج هاتفك أن يكون مشغلاً
- ✅ سريع وموثوق

### الطريقة 2: تشغيل على الهاتف (للتجربة فقط)
**الملاحظات:**
- ⚠️ يستهلك البطارية
- ⚠️ يتوقف عند إغلاق الهاتف
- ⚠️ يحتاج هاتف متصل بالإنترنت دائماً

---

# 🌟 الطريقة الأولى: Render.com (مجاني 100%)

## الخطوة 1️⃣: إنشاء حساب على Render

1. افتح المتصفح على هاتفك
2. اذهب إلى: **https://render.com**
3. اضغط على **"Get Started"** أو **"Sign Up"**
4. سجل باستخدام:
   - GitHub (موصى به)
   - أو GitLab
   - أو البريد الإلكتروني

## الخطوة 2️⃣: إنشاء البوت على تليغرام

1. **افتح تليغرام** على هاتفك
2. ابحث عن: **@BotFather**
3. افتح المحادثة واضغط **"Start"**
4. أرسل الأمر: `/newbot`
5. **اختر اسم البوت** (مثال: بوت إدارة التخطيط البريقة)
6. **اختر معرف البوت** (يجب أن ينتهي بـ bot)
   - مثال: `BuraigaPlanningBot`
7. **احفظ التوكن** الذي سيرسله لك
   - يشبه: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`

## الخطوة 3️⃣: رفع الملفات إلى GitHub

### أ. إنشاء حساب GitHub (إذا لم يكن لديك)

1. افتح: **https://github.com**
2. اضغط **"Sign up"**
3. أكمل التسجيل

### ب. إنشاء مستودع (Repository)

1. بعد تسجيل الدخول، اضغط **"+"** أعلى الشاشة
2. اختر **"New repository"**
3. **اسم المستودع**: `buraiga-planning-bot`
4. اختر **"Private"** (خاص)
5. ✅ فعّل **"Add a README file"**
6. اضغط **"Create repository"**

### ج. رفع ملفات البوت

**من الكمبيوتر (إذا متاح):**

```bash
# فك ضغط الملف المحمل
tar -xzf telegram_bot_complete.tar.gz
cd telegram_bot

# تهيئة Git
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/YOUR_USERNAME/buraiga-planning-bot.git
git push -u origin main
```

**من الهاتف مباشرة:**

1. حمل تطبيق **"GitHub"** من Play Store
2. افتح المستودع الذي أنشأته
3. اضغط **"+"** → **"Upload files"**
4. قم برفع جميع ملفات البوت (بعد فك الضغط)

## الخطوة 4️⃣: ربط Render بـ GitHub

1. افتح **https://render.com**
2. اضغط **"New +"** → **"Web Service"**
3. اضغط **"Connect GitHub"**
4. اختر المستودع: `buraiga-planning-bot`
5. اضغط **"Connect"**

## الخطوة 5️⃣: إعدادات Render

### في صفحة الإعدادات:

**1. Name:** `buraiga-planning-bot`

**2. Region:** اختر الأقرب لك
   - Singapore (للشرق الأوسط)
   - أو Frankfurt

**3. Branch:** `main`

**4. Root Directory:** (اتركه فارغاً)

**5. Runtime:** `Python 3`

**6. Build Command:**
```bash
pip install -r requirements.txt
```

**7. Start Command:**
```bash
python main.py
```

**8. Instance Type:** 
   - اختر **"Free"** (مجاني)

### إضافة المتغيرات البيئية (Environment Variables):

اضغط **"Add Environment Variable"** وأضف:

**Key:** `BOT_TOKEN`  
**Value:** `التوكن الذي حصلت عليه من BotFather`

**Key:** `DATABASE_URL`  
**Value:** `sqlite:///buraiga_planning.db`

## الخطوة 6️⃣: نشر البوت

1. اضغط **"Create Web Service"**
2. انتظر 2-3 دقائق للنشر
3. ستظهر رسالة **"Live"** عندما يكون البوت جاهزاً

## الخطوة 7️⃣: تفعيل البوت

⚠️ **مهم جداً:**

Render يوقف الخدمات المجانية بعد 15 دقيقة من عدم الاستخدام.

**الحل:**

استخدم خدمة Cron Job مجانية:

1. افتح: **https://cron-job.org**
2. سجل حساب مجاني
3. اضغط **"Create Cron Job"**
4. **Title:** `Keep Buraiga Bot Alive`
5. **URL:** `https://YOUR-APP-NAME.onrender.com`
6. **Schedule:** كل 10 دقائق
7. احفظ

الآن البوت سيعمل 24/7! 🎉

---

# 📱 الطريقة الثانية: التشغيل على الهاتف مباشرة

## المتطلبات:

1. هاتف أندرويد (4GB RAM على الأقل)
2. مساحة فارغة 500 MB
3. إنترنت مستقر

## الخطوات:

### 1. تثبيت Termux

1. افتح **F-Droid** (حمله من: https://f-droid.org)
2. ابحث عن **"Termux"**
3. ثبت التطبيق

⚠️ **لا تثبت Termux من Play Store** (نسخة قديمة)

### 2. تثبيت Python

افتح Termux واكتب:

```bash
# تحديث الحزم
pkg update && pkg upgrade -y

# تثبيت Python و Git
pkg install python git -y

# تثبيت مكتبات إضافية
pkg install libxml2 libxslt -y
```

### 3. تحميل ملفات البوت

```bash
# إنشاء مجلد
mkdir ~/buraiga-bot
cd ~/buraiga-bot

# إذا رفعت على GitHub:
git clone https://github.com/YOUR_USERNAME/buraiga-planning-bot.git
cd buraiga-planning-bot

# أو حمل الملف المضغوط ثم:
# (ضع الملف في مجلد Downloads أولاً)
cp ~/storage/downloads/telegram_bot_complete.tar.gz .
tar -xzf telegram_bot_complete.tar.gz
cd telegram_bot
```

### 4. إعداد البوت

```bash
# نسخ ملف الإعدادات
cp .env.example .env

# تحرير ملف الإعدادات
nano .env
```

**في nano:**
- أضف التوكن من BotFather
- اضغط **Ctrl + X**
- ثم **Y**
- ثم **Enter**

### 5. تثبيت المكتبات

```bash
pip install -r requirements.txt
```

قد تستغرق 5-10 دقائق

### 6. تهيئة قاعدة البيانات

```bash
python database.py
```

يجب أن ترى: ✅ تم إنشاء قاعدة البيانات بنجاح

### 7. تشغيل البوت

```bash
python main.py
```

يجب أن ترى:
```
✅ تم تهيئة قاعدة البيانات
🤖 بدء تشغيل البوت...
✅ البوت جاهز للعمل!
```

### 8. إبقاء البوت يعمل في الخلفية

```bash
# تثبيت tmux
pkg install tmux -y

# إنشاء جلسة
tmux new -s bot

# تشغيل البوت
python main.py

# للانفصال عن الجلسة:
# اضغط: Ctrl + B ثم D

# للعودة للجلسة:
tmux attach -t bot
```

### 9. تشغيل تلقائي عند فتح Termux

```bash
# إنشاء سكريبت
cat > ~/.bashrc << 'EOF'
# بدء البوت تلقائياً
if [ -z "$TMUX" ]; then
    cd ~/buraiga-bot/telegram_bot
    tmux attach -t bot || tmux new -s bot "python main.py"
fi
EOF
```

---

# 🌐 الطريقة الثالثة: استخدام Railway.app

## مميزات Railway:

- ✅ مجاني تماماً (500 ساعة/شهر)
- ✅ قاعدة بيانات PostgreSQL مجانية
- ✅ سهل جداً

## الخطوات:

### 1. إنشاء حساب

1. افتح: **https://railway.app**
2. اضغط **"Login"** → **"Login with GitHub"**
3. وافق على الأذونات

### 2. إنشاء مشروع جديد

1. اضغط **"New Project"**
2. اختر **"Deploy from GitHub repo"**
3. اختر المستودع: `buraiga-planning-bot`

### 3. إضافة قاعدة بيانات (اختياري)

1. في لوحة المشروع، اضغط **"+ New"**
2. اختر **"Database"** → **"PostgreSQL"**
3. سيُنشأ تلقائياً

### 4. ربط قاعدة البيانات

1. في خدمة البوت، اذهب لـ **"Variables"**
2. أضف:
   - `BOT_TOKEN`: التوكن من BotFather
   - `DATABASE_URL`: سينسخ تلقائياً من PostgreSQL

### 5. النشر

البوت سيُنشر تلقائياً! 🎉

---

# 🔗 الطريقة الرابعة: Heroku (موثوق جداً)

⚠️ **ملاحظة:** Heroku ألغى الخطة المجانية، لكن يمكنك استخدام الخطة المدفوعة ($5/شهر)

## إذا قررت استخدام Heroku:

### 1. إنشاء ملفات إضافية

أولاً، أنشئ ملف `Procfile`:

```bash
echo "worker: python main.py" > Procfile
```

أنشئ `runtime.txt`:

```bash
echo "python-3.11.0" > runtime.txt
```

### 2. رفع على GitHub

```bash
git add Procfile runtime.txt
git commit -m "Add Heroku files"
git push
```

### 3. ربط مع Heroku

1. افتح: **https://heroku.com**
2. سجل/سجل دخول
3. اضغط **"New"** → **"Create new app"**
4. اختر اسم: `buraiga-planning-bot`
5. في **"Deployment method"** اختر **"GitHub"**
6. ابحث عن المستودع واربطه
7. فعّل **"Automatic deploys"**

### 4. إضافة المتغيرات

1. اذهب لـ **"Settings"** → **"Config Vars"**
2. اضغط **"Reveal Config Vars"**
3. أضف:
   - `BOT_TOKEN`: التوكن
   - `DATABASE_URL`: postgresql://...

### 5. تغيير نوع Dyno

1. اذهب لـ **"Resources"**
2. أوقف **"web"**
3. فعّل **"worker"**

---

# ✅ التحقق من عمل البوت

## اختبار البوت:

1. **افتح تليغرام** على هاتفك
2. **ابحث عن البوت** بالمعرف الذي اخترته
   - مثال: `@BuraigaPlanningBot`
3. **اضغط "Start"**
4. يجب أن يرد البوت برسالة الترحيب

## إذا لم يرد البوت:

### تحقق من السجلات (Logs):

**في Render:**
- اذهب لصفحة الخدمة
- اضغط **"Logs"**

**في Railway:**
- اضغط على الخدمة
- اضغط **"View Logs"**

**في Termux:**
```bash
# الأخطاء تظهر مباشرة في الشاشة
```

---

# 🛠️ حل المشاكل الشائعة

## المشكلة 1: البوت لا يرد

**الحل:**
1. تأكد من صحة التوكن
2. تحقق من السجلات
3. تأكد أن الخدمة "Live"

## المشكلة 2: خطأ في قاعدة البيانات

**الحل:**
```bash
# حذف قاعدة البيانات القديمة
rm buraiga_planning.db

# إعادة الإنشاء
python database.py
```

## المشكلة 3: البوت يتوقف على Render

**الحل:**
- استخدم Cron Job كما شُرح أعلاه
- أو استخدم Railway/Heroku

## المشكلة 4: نفذت الساعات المجانية

**الحلول:**
1. استخدم Render + Railway معاً (موازنة)
2. استخدم خدمات متعددة بالتناوب
3. ترقية للخطة المدفوعة

---

# 📊 مقارنة الخدمات

| الخدمة | مجاني | ساعات | قاعدة بيانات | السهولة |
|--------|------|-------|--------------|---------|
| Render | ✅ | 750 ساعة/شهر | SQLite | ⭐⭐⭐⭐⭐ |
| Railway | ✅ | 500 ساعة/شهر | PostgreSQL | ⭐⭐⭐⭐ |
| Heroku | ❌ ($5/شهر) | غير محدود | PostgreSQL | ⭐⭐⭐⭐⭐ |
| Termux | ✅ | غير محدود | SQLite | ⭐⭐⭐ |

---

# 🎯 التوصية النهائية

**للاستخدام الفعلي:**
1. **الأفضل:** Render + Cron Job
2. **البديل:** Railway
3. **الأكثر موثوقية:** Heroku (مدفوع)

**للتجربة فقط:**
- Termux على الهاتف

---

# 🔐 نصائح الأمان

1. **لا تشارك التوكن** مع أحد
2. استخدم **Environment Variables** دائماً
3. اجعل المستودع **Private** على GitHub
4. فعّل **Two-Factor Authentication** على GitHub
5. غيّر التوكن إذا تسرب

---

# 📞 الدعم

إذا واجهت مشاكل:

1. راجع قسم "حل المشاكل" أعلاه
2. تحقق من السجلات (Logs)
3. راجع ملف `README.md`

---

✨ **البوت جاهز للعمل على هاتفك الأندرويد! استمتع!** ✨
