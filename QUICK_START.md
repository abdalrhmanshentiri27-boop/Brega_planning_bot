# ⚡ دليل البدء السريع - 5 دقائق

## 🎯 من الصفر للتشغيل في 5 خطوات

### الخطوة 1️⃣: تثبيت المتطلبات (دقيقة واحدة)

```bash
# تأكد من وجود Python 3.8+
python3 --version

# ثبت المكتبات
pip3 install -r requirements.txt
```

### الخطوة 2️⃣: إنشاء البوت على تليغرام (دقيقة واحدة)

1. افتح [@BotFather](https://t.me/BotFather)
2. أرسل: `/newbot`
3. اتبع التعليمات
4. **احفظ التوكن**

### الخطوة 3️⃣: إعداد التوكن (30 ثانية)

```bash
# انسخ ملف الإعدادات
cp .env.example .env

# افتح الملف
nano .env

# الصق التوكن
BOT_TOKEN=التوكن_هنا
```

### الخطوة 4️⃣: تهيئة قاعدة البيانات (30 ثانية)

```bash
python3 database.py
```

### الخطوة 5️⃣: تشغيل البوت (30 ثانية)

```bash
python3 main.py
```

---

## ✅ الآن البوت يعمل!

افتح البوت على تليغرام وأرسل `/start`

---

## 🚀 التشغيل التلقائي بأمر واحد

أو استخدم سكريبت التثبيت:

```bash
chmod +x setup.sh
./setup.sh
```

---

## 📱 الخطوات التالية

1. **سجل نفسك** كمستخدم أول
2. **منح صلاحيات** (الإدارة العليا للمستخدم الأول)
3. **ابدأ الاستخدام** - أضف أول خطة أو مشروع!

---

## ❓ مشاكل؟

### البوت لا يرد؟
```bash
# تحقق من التوكن
cat .env

# تحقق من السجلات
tail -f bot.log
```

### خطأ في المكتبات؟
```bash
pip3 install --upgrade pip
pip3 install -r requirements.txt --force-reinstall
```

---

## 📚 المزيد من التفاصيل

- **دليل التثبيت الكامل**: `INSTALLATION_GUIDE.md`
- **دليل المستخدم**: `USER_GUIDE.md`
- **الأسئلة الشائعة**: `README.md`

---

**🎉 مبروك! بوت إدارة التخطيط جاهز للعمل!**
